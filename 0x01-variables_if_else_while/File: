#include<stdlib.h>
#include<time.h>
#include<stdio.h>
/**
 * main-Determine if number is positive ,negative or zero
 *
 * Return Always 0(Success)
 */
int main (void)
{ 
putchar(n);
if(n!=57)
{
putchar(',');
putchar('');
}
putchar('\n');
return(0);
}
